<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.netposa.gat.mapper.DeviceMapper">
    <select id="getAll" resultType="com.netposa.gat.model.tujie.ape.DeviceObj">
        SELECT
        a.id,
        a.NAME,
        a.cameraType,
        a.pinyin,
        a.acronym,
        a.longitude,
        a.latitude,
        a.isOnline,
        a.direction,
        a.gbId
        FROM proxy_camera a
    </select>

    <select id="getDeviceObjById" parameterType="String" resultType="com.netposa.gat.model.tujie.ape.DeviceObj">
        SELECT
        id,
        NAME,
        cameraType,
        pinyin,
        acronym,
        longitude,
        latitude,
        isOnline,
        direction,
        FROM proxy_camera
        WHERE id = #{id}
    </select>
</mapper>